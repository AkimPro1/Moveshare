# MoveShare Frontend (Vite + React + TypeScript)

Quick start (PowerShell):

```powershell
cd moveshare-frontend
npm install
npm run dev
```

Open `http://localhost:5173` in your browser. The frontend expects the API at `http://127.0.0.1:8000/api`.
